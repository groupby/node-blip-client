platform: linux

image_resource:
  type: docker-image
  source: {repository: node, tag: "7"}

inputs:
  - name: node-blip-client

outputs:
  - name: installed

run:
  path: bash
  args: [ -c, 'cd node-blip-client && npm install' ]

