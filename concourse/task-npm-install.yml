platform: linux

image_resource:
  type: docker-image
  source: {repository: node, tag: "7"}

inputs:
  - name: node-blip-client

run:
  path: npm
  args:
  - install
    &&
    npm
    install
    -g
    gulp-cli
